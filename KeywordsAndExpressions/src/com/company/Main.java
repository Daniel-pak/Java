package com.company;

public class Main {

    public static void main(String[] args) {
        //expressions are building blocks of all java programs
        double kilometres = (100 * 1.609344); //the thing in the bracket is a statement, variable + operator + data
        int highScore = 50;
        if (highScore == 50) {
            System.out.println("This is an expression");
        }
    }
}
